---
// Marks JS as available so reveal animations can safely hide content until
// they run. Without JS, content stays visible (see global.css). The site is
// dark-only, so there is no theme to set before paint.
---
<script is:inline>
  document.documentElement.classList.add("js");
</script>
